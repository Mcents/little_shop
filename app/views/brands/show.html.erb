
<%= @brand.name %></br>
<%= if current_user.admin? %>
  <%= button_to "Edit", edit_brand_path(@brand) %>
  <%= button_to "Delete", brand_path(@brand), method: :delete %>
<%= end %>

<% @brand_products.each do |p|%>
<%= p.name %>
<%= p.description %>
<%= p.price %></br>

<% end %>
