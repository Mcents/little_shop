
<% @order.product.each do |product| %>
<%= link_to product.name, product_path(product) %>

$<%= product.price %><br><br>
<%= product.quantity %><br><br>
<%= product.total_price %><br><br>
<%= @order.created_at %>
<%= @order.status %>

  <% if @order.status == 2 || @order.status == 3 %>
    <%= @order.updated_at %>
    <% end %><br><br>
<% end %>
